# Contributor notes

- Keep preprocessing identical in training and inference.
- Do not train on the public test set.
- Prefer a measured small model over an unverified larger one.
- Record every experiment in `docs/experiment_log.md`.
