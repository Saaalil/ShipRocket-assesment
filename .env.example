# Copy to .env locally. Never commit real tokens.

# Optional: Hugging Face write token for publishing models/spaces
HF_TOKEN=

# Optional: disable online dataset downloads during tests
SMART_TURN_OFFLINE=0

# Optional: local path to an exported ONNX model
SMART_TURN_ONNX_PATH=
